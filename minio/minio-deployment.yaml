apiVersion: apps/v1
kind: Deployment
metadata:
  name: minio
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minio
  template:
    metadata:
      labels:
        app: minio
    spec:
      containers:
      - name: minio
        image: minio/minio
        args:
          - server
          - /data
        envFrom:
        - secretRef:
            name: minio-credentials
        ports:
        - containerPort: 9000
